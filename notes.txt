- engine/* : depending on SDL3
- engine/window.h : depending on glew
- engine/inputs.h : inputs_get() can only be used for one input at time
- engine/inputs.h : errors are handled via assert
- gl/* : depending on glew
- gl/* : no real error checks and protections
- gl/texture.c : depending on stb_image.h
- gl/texture.c : hard coded RGBA only textures
- gl/buffer.c : vao is considered as a glbuffer
- src/sprites.c : sprites_registerTexture() can't exists with other textures handling functions
- src/sprites.c : registered textures have fixxed parameter
- src/main.c : accessing 'sprites->shader'
